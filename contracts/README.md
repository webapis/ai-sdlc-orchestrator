# Contracts

Node and tool contracts: required inputs, produced outputs, allowed
tools, timeout, retry policy, side effects, failure categories,
validation rules, and ownership — for every node type used across
`/workflows`. See `docs/architecture.md` §3, "Node contracts" under
Production Requirements.

Not yet populated. Add one contract file per node/tool as the roadmap
Phase 1 role set (`planner`, `coder`, `reviewer`) is implemented.
