# Kubernetes Manifests

See `docs/architecture.md` §15 for the target container layout
(`orchestrator-api`, `orchestrator-engine`, `orchestrator-worker`,
`postgres`, `redis`) and scaling/PodDisruptionBudget notes.

Not yet written — add manifests here once Phase 4 (Multi-Tenant &
Production Hardening) begins per the roadmap.
